services:
  lms-web:
    build: .
    container_name: lms-web
    env_file:
      - .env
    command: sh -c "npm run dev -- --host"
    volumes:
      - .:/app
      - /app/node_modules
    networks:
      - nginx-proxy
networks:
  nginx-proxy:
    external: true
