services:
  lms-web:
    image: lms-web:latest
    container_name: lms-web
    ports:
      - ${PORT}:80
    environment:
      - VITE_API_BASE_URL=${VITE_API_BASE_URL}
    restart: always
    networks:
      - lms-network

networks:
  lms-network:
    external: true
